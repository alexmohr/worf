[package]
name = "worf-warden"
version = "0.1.0"
edition = "2024"

[dependencies]
worf = {path = "../../worf"}
env_logger = "0.11.8"

# todo re-add this
#[features]
#default = []  # nothing enabled by default
#warden = ["worf-warden"]
