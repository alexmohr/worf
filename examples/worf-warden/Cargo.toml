[package]
name = "worf-warden"
version = "0.1.0"
edition = "2024"

[dependencies]
worf = {path = "../../worf"}
anyhow = "1.0.98"

# todo re-add this
#[features]
#default = []  # nothing enabled by default
#warden = ["worf-warden"]
